/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Включает строгий режим React для выявления потенциальных проблем
    swcMinify: true,       // Включает использование нового компилятора SWC для минификации
};

